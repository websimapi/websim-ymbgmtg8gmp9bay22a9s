/* old start text / leaderboard styles removed; kept file minimal for compatibility */

/* legacy hidden class no longer used for start screen */
.hidden {
  display: none;
  pointer-events: none;
}

